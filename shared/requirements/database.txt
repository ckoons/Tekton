# Database requirements for Tekton components
# Used by: Budget, Ergon, Harmonia, Hermes, Metis, ESR

# Core database tools
sqlalchemy>=2.0.0        # SQL toolkit and ORM
alembic>=1.12.0         # Database migrations

# Database drivers for ESR system
aiosqlite>=0.19.0       # Async SQLite adapter for relational storage
redis>=5.0.0            # Redis client for cache storage (optional)
tinydb>=4.8.0           # Lightweight document database

# Optional database drivers (uncomment as needed)
# psycopg2-binary>=2.9.0  # PostgreSQL adapter
# pymysql>=1.1.0          # MySQL adapter